{
  "name": "koa-view",
  "version": "2.1.2",
  "description": "koa view use nunjucks",
  "main": "index.js",
  "scripts": {
    "test": "mocha --exit -R spec",
    "report": "istanbul cover _mocha -- --exit -R spec",
    "coveralls": "npm run report && cat ./coverage/lcov.info | coveralls && rm -rf ./coverage"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/d-band/koa-view.git"
  },
  "keywords": [
    "koa",
    "view",
    "nunjucks"
  ],
  "author": "d-band",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/d-band/koa-view/issues"
  },
  "homepage": "https://github.com/d-band/koa-view#readme",
  "dependencies": {
    "nunjucks": "^3.1.7"
  },
  "devDependencies": {
    "coveralls": "^3.0.2",
    "istanbul": "^0.4.5",
    "koa": "^2.7.0",
    "mocha": "^6.0.0",
    "should": "^13.2.3",
    "supertest": "^3.4.2"
  }
}
